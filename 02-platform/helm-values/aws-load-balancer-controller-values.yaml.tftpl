# Helm values for aws-load-balancer-controller.
# Rendered by Terraform with templatefile(); do not replace the ${...} placeholders here.

clusterName: ${cluster_name}
region: ${aws_region}
vpcId: ${vpc_id}
replicaCount: ${replica_count}

serviceAccount:
  create: true
  name: aws-load-balancer-controller
